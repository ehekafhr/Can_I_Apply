# 지원해도 될까요? — 프로젝트 계획서

> "신입" 필터를 걸어도 섞여 나오는 "신입+경력" 통합공고를 **직무 단위로 분해**해서,
> 내가 지원하려는 직무가 진짜 "신입"인 공고만 검색되게 하는 서비스.

작성일: 2026-07-17

---

## 1. 문제 정의

- 사람인·잡알리오에서 "신입"으로 필터링해도 **공고 단위**로 태그가 붙기 때문에,
  하나의 공고에 신입 직무와 경력 직무가 섞여 있으면 그대로 노출된다.
- 내가 원하는 직무만 "경력"인 경우, 공고를 일일이 열어 첨부된 공고문(대부분 이미지/PDF/HWP)을
  읽어야만 알 수 있다.
- **해법**: 공고 첨부문서를 AI로 분석해 공고를 **직무(position) 단위로 분할**하여 DB에 저장하고,
  직무 단위 경력구분으로 검색하게 한다.

## 2. 범위 결정 (데이터 소스)

### 1차 (MVP): 잡알리오 — 공공기관 채용정보 ✅
- **크롤링 불필요.** 공공데이터포털에 공식 오픈API가 있다:
  - [재정경제부_공공기관 채용정보 조회서비스](https://www.data.go.kr/data/15125273/openapi.do)
    — 공고 제목, 접수기간, 채용직무·인원, 전형단계, 접수방법 + **첨부파일 메타정보** 제공.
    REST / JSON·XML, 무료. 개발계정 트래픽 일 1,000건.
  - 알리오 자체 개방데이터 포털도 존재: [opendata.alio.go.kr](https://opendata.alio.go.kr/recruit/list)
- 첨부파일 메타정보로 공고문 원본(이미지/PDF/HWP)을 받아 분석한다. → 합법·안정적.

### 2차 (확장): 사람인
- **직접 크롤링 금지**: robots.txt가 `/zf_user/recruit/`, `recruit_view.php` 등
  채용공고 상세를 크롤러에 차단하고 있음.
- 공식 [사람인 오픈API](https://oapi.saramin.co.kr/) 사용 (이용신청·승인 필요, 일 500회 제한).
- MVP 완성 후 API 승인을 받아 붙이는 것으로 미룬다.

## 3. 시스템 구성 (파이프라인)

```
[① Collector]  오픈API 주기 호출 → 신규 공고 + 첨부파일 다운로드
      ↓
[② Extractor]  첨부문서 → AI 분석 → 직무 단위 구조화(JSON)
      ↓
[③ DB]         공고 1 : N 직무 로 분할 저장
      ↓
[④ Search]     "경력구분 = 신입" 직무 단위 검색 API + UI
```

### ① 수집기 (Collector)
- 공공데이터포털 API를 하루 1~2회 호출(cron), 신규/변경 공고만 수집 (idempotent upsert).
- 첨부파일 다운로드 시 요청 간 딜레이, 재시도, 로컬 캐시.
- 트래픽 한도(일 1,000건) 내에서 페이지네이션.

### ② 추출기 (Extractor) — 이 프로젝트의 핵심
파일 유형별 분기:
- **PDF**: 텍스트 레이어가 있으면 바로 추출, 없으면(스캔본) 이미지로 렌더링 후 AI 분석.
- **이미지(JPG/PNG)**: AI 비전 모델로 바로 구조화 추출.
- **HWP/HWPX**: 공공기관 특성상 비중이 클 것 → `hwp5txt`/`pyhwpx` 텍스트 변환,
  실패 시 PDF 변환 → 이미지 경로로 폴백.

이미지 분석 접근 (선택지):

| 옵션 | 방식 | 장점 | 단점 |
|---|---|---|---|
| **A (추천)** | VLM API (Claude vision 등)로 이미지→구조화 JSON 한 번에 | 개발 빠름, 한국어·표 구조에 강함, 라벨링 불필요 | 호출 비용 |
| B | 오픈소스 OCR(PaddleOCR 등) + LLM 후처리 | 저비용 | 한국어 표/레이아웃 인식 품질, 파이프라인 복잡 |
| C | 레이아웃 검출 + OCR 모델 직접 학습 | ML 학습 경험 | 라벨링 데이터 구축 비용 큼, MVP에 과함 |

→ **MVP는 A로 시작**하고, 프로젝트 목적에 "모델을 직접 만드는 학습"이 포함된다면
  A로 만든 추출 결과를 라벨 데이터 삼아 C를 2단계로 진행하는 경로가 효율적.

추출 출력 스키마(직무 단위):
```json
{
  "positions": [
    {
      "title": "전산직 (정보보안)",
      "employment_type": "정규직",
      "career_level": "신입 | 경력 | 신입/경력 | 무관",
      "career_detail": "경력 3년 이상 등 원문 요건",
      "headcount": 2,
      "location": "나주",
      "requirements": "관련 자격증 ...",
      "evidence": "판단 근거가 된 원문 문장",
      "confidence": 0.95
    }
  ]
}
```
- `evidence` + `confidence`를 반드시 저장: 오추출로 공고를 잘못 걸러내는 걸 방지하고,
  낮은 confidence는 UI에서 "확인 필요"로 표시.

### ③ DB 스키마 (초안)
- `announcements` — 공고 원본 메타 (API id, 기관, 제목, 접수기간, 원문 URL, 수집시각)
- `attachments` — 첨부파일 (공고 FK, 파일 URL, 유형, 로컬 경로, 해시)
- `positions` — **분할된 직무** (공고 FK, 직무명, 경력구분, 인원, 근무지, evidence, confidence)
- `extraction_runs` — 추출 이력 (모델, 프롬프트 버전, 비용, 성공여부) — 재추출/평가용
- 시작은 **SQLite**, 필요해지면 PostgreSQL 이전 (SQLAlchemy로 추상화).

### ④ 검색/서빙
- FastAPI 백엔드 + 가벼운 웹 UI (초기엔 단일 페이지면 충분).
- 필터: 경력구분(신입만 / 신입 지원 가능 포함), 직무 키워드, 지역, 접수 마감 임박순.
- 결과에는 항상 잡알리오 원문 링크를 함께 표시 (책임 있는 안내).

## 4. 크롤링/이용 규칙 준수

1. 잡알리오: 크롤링 대신 **공식 오픈API만** 사용. 트래픽 한도 준수, 응답 캐싱.
2. 첨부파일: API가 제공하는 메타 기준으로만 다운로드, 요청 간 딜레이(예: 1초+).
3. 사람인: robots.txt상 상세 페이지 크롤링 금지 → **공식 API 승인 후에만** 연동.
4. 수집 데이터는 개인 용도/포트폴리오 범위로 사용, 재배포 시 출처 표기.

## 5. 기술 스택 (제안)

- Python 3.12 / httpx / pydantic (추출 스키마 검증)
- FastAPI + SQLAlchemy + SQLite
- 문서 처리: pypdf / pdf2image / hwp5txt
- AI 추출: Claude API (vision, structured output) — 프롬프트+스키마 버전 관리
- 스케줄링: cron (WSL) 또는 APScheduler

## 6. 마일스톤

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **M0** | API 키 발급 + 실제 응답 필드 검증 | 샘플 공고 10건의 JSON과 첨부 메타를 눈으로 확인 |
| **M1** | Collector + DB | 최근 공고 전체가 announcements/attachments에 적재 |
| **M2** | Extractor | 수동 라벨 평가셋 30~50건에서 경력구분 정확도 ≥ 95% |
| **M3** | 검색 API + UI | "신입만" 필터로 직무 단위 검색 동작 |
| **M4** | 스케줄러 + 운영 | 매일 자동 수집·추출, 실패 알림 |
| M5 | (확장) 사람인 API 연동 | — |

## 7. 리스크 & 검증 포인트

- **M0가 최우선 검증**: API 응답에 이미 공고 단위 신입/경력 필드가 있는지, 첨부파일
  URL이 실제로 받아지는지, HWP 비중이 얼마나 되는지 — 이 결과에 따라 M2 설계가 조정됨.
- HWP 파싱 실패율이 높으면: hwp→pdf 변환 서버(libreoffice/hancom docs converter) 검토.
- VLM 비용: 공고당 이미지 수 × 호출 단가로 월 비용 추정 후, 신규 공고만 증분 처리로 억제.
- 오추출 리스크: "신입인데 경력으로 분류되어 안 보이는" false negative가 최악 →
  애매하면 '신입 가능'으로 분류하는 프롬프트 정책 + confidence 낮은 건 노출.

## 8. 사용자가 해야 할 일 (코드 밖)

- [ ] [공공데이터포털](https://www.data.go.kr/data/15125273/openapi.do) 회원가입 → 해당 API 활용신청 → 인증키 발급
- [ ] (M2 전) Claude API 키 준비 (또는 옵션 B/C 선택 시 불필요)
- [ ] (M5 전) 사람인 오픈API 이용신청
